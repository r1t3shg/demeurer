# Demeurer environment variables.
# Copy this file to `.env` and fill in real values. `.env` is gitignored.

SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SCOPES=
SHOPIFY_APP_URL=
HOST=
DATABASE_URL=

# Optional: only when developing against a custom dev-store domain.
# SHOP_CUSTOM_DOMAIN=
